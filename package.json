{
  "name": "middle.messenger.praktikum.yandex",
  "version": "1.0.0",
  "description": "Start of sprint #1",
  "source": "index.html",
  "scripts": {
    "clean-dist": "rm -rf dist && mkdir dist",
    "copy-files": "cp -vR ./src/images ./dist && cp -v _redirects ./dist",
    "init": "npm run clean-dist && npm run copy-files",
    "start": "npm run build && node ./server.js",
    "build": "npm run init && parcel build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skaamoogs/middle.messenger.praktikum.yandex.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/skaamoogs/middle.messenger.praktikum.yandex/issues"
  },
  "homepage": "https://github.com/skaamoogs/middle.messenger.praktikum.yandex#readme",
  "devDependencies": {
    "@anikin/parcel-transformer-handlebars": "^1.0.9",
    "@inventory/parcel-plugin-handlebars": "^1.0.0",
    "@parcel/transformer-sass": "^2.7.0",
    "express": "^4.18.2",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "handlebars": "^4.7.7",
    "sass": "^1.55.0"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "targets": {
    "default": {
      "engines": {
        "browsers": ">= 50%"
      }
    }
  }
}
