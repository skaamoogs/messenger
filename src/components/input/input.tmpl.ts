const inputTemplate = `
    <input 
      class="input" 
      id="{{ name }}" 
      type="{{ type }}" 
      name="{{ name }}" 
      placeholder="{{placeholder}}">
`;

export default inputTemplate;
