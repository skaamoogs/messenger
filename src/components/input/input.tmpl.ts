const inputTemplate = `
    <input 
      class="input {{className}}" 
      id="{{ name }}" 
      type="{{ type }}" 
      name="{{ name }}" 
      placeholder="{{placeholder}}"
      value="{{value}}"
      accept="{{accept}}">
`;

export default inputTemplate;
